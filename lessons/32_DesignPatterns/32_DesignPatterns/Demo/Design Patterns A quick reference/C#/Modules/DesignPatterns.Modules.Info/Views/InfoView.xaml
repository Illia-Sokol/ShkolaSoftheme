<UserControl x:Class="DesignPatterns.Modules.Info.Views.InfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="80" Width="900">

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Button ToolTip="Return Home" Style="{StaticResource ButtonStyle}"
                Command="{Binding HomeCommand}" Background="{StaticResource HomeButtonBackgroundImage}">
        </Button>
        <Button ToolTip="About Me!" Style="{StaticResource ButtonStyle}"  Background="{StaticResource InfoButtonBackgroundImage}">
        </Button>
    </StackPanel>
</UserControl>
